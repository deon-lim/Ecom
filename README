Start up Redis (using docker)
docker run -d --name redis -p 6379:6379 redis

Installing and running consul to manage services 
1. Install consul
docker pull hashicorp/consul

2. Run consul (using docker)
docker run -d --name=consul -p 8500:8500 hashicorp/consul

Shut down all docker containers
docker stop $(docker ps -q)
